 if array.length == 1 
   "#{array[0]}"
  end




if array.length == 2 
    "#{array.join(" and ")}"
  end 
  



if array.length > 1 
    store = ", and #{array[-1]}"
    array.delete(-1)
    string = array.join(", ") 
    string << store
  end 